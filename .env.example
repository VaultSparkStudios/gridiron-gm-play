# Copy to .env.local and fill in values

# Analytics endpoint (optional) — privacy-safe beacon URL, no PII sent
# Accepts POST with JSON: { e: eventName, v: version, t: timestamp }
# Leave empty to disable analytics
VITE_ANALYTICS_URL=
